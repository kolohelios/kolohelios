# Generated by shaka. Do not edit by hand.
# Run `shaka ci generate-workflows` to refresh.
#
# Source of truth: <slot>/<project>/project.cue.

name: notes-web cleanup
on:
  pull_request:
    types:
    - closed
permissions:
  id-token: write
  contents: read
jobs:
  cleanup:
    runs-on: ubuntu-latest
    env:
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    steps:
    - name: verify OP_SERVICE_ACCOUNT_TOKEN
      run: |
        if [[ -z "$OP_SERVICE_ACCOUNT_TOKEN" ]]; then
          echo "::error::OP_SERVICE_ACCOUNT_TOKEN is empty; check repo/org secret"
          exit 1
        fi
    - uses: actions/checkout@v6
    - uses: DeterminateSystems/nix-installer-action@main
      with:
        determinate: true
        flakehub: true
    - uses: DeterminateSystems/flakehub-cache-action@v3
    - name: wrangler delete preview Worker
      run: |
        cd apps/notes-web
        cp .env.example .env
        nix develop . --command op run --env-file=.env -- \
          wrangler delete \
          --name notes-web-pr-${{ github.event.pull_request.number }} \
          --force \
          || true
