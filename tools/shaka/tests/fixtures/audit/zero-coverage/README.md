# zero-coverage fixture

Rust project with `coverage.line.fail: 0` — should fail rust-coverage-threshold-nonzero.
(Branch is allowed to be zero; only line is gated. See `zero-branch-coverage` for the
positive case.)
