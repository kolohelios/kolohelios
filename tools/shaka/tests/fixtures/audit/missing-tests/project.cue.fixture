package project

#Project & {
	name: "missing-tests"
	kind: "rust-cli"
	cli: {
		binaryName: "missing-tests"
		package:    false
	}
	coverage: {
		line: {
			fail: 30
		}
		branch: {
			fail: 20
		}
	}
	audit: {
		overrides: [
			{
				rule:          "rust-cli-package-false-requires-override"
				severity:      "off"
				justification: "test fixture; this rule is not under test"
			},
		]
	}
}
