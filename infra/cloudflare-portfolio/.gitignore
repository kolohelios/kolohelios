# Terraform / OpenTofu local state and plan artifacts
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Project-local secret references (only `.env.example` is committed).
.env

# Wrangler local artifacts.
.wrangler/

# Nix build outputs (`nix build` symlinks).
result
result-*
